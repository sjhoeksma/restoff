describe ("restoffAngularProvider", function() {

	it("01: should have angular setup", function() {
		expect(typeof angular !== "undefined", "angular.js is required for these tests").to.be.true;
	});

});
